apiVersion: v1
kind: Service
metadata:
  name: {{ include "harphies-apps.fullname" . }}
  labels:
    {{- include "harphies-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "harphies-apps.selectorLabels" . | nindent 4 }}
