apiVersion: v1
kind: Service
metadata:
  name: {{ include "harphies-app.fullname" . }}
  labels:
    {{- include "harphies-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "harphies-app.selectorLabels" . | nindent 4 }}
